// import Point from "ol/geom/Point.js";

const actions = {
    // NOTE write actions here if you need them
    // addFeatureToRoutingLayer ({state}, obj) {
    //     const cbFunction = obj.cbFunction,
    //         feature = obj.feature;

    //     feature.geometry = new Point(feature.coordinates);
    //     cbFunction(feature);
    // }
};

export default actions;
